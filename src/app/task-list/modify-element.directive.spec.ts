import { ModifyElementDirective } from './modify-element.directive';

describe('ModifyElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ModifyElementDirective();
    expect(directive).toBeTruthy();
  });
});
